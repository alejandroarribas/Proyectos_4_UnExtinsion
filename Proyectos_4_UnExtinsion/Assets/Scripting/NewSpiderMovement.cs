using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class NewSpiderMovement : MonoBehaviour
{
    public float Range = 0.5f;

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        //Rayo calculador des suelo(ver que el suelo esta y tratar de hacer que la rotación del ojeto este con la normal)

        //Rayo para detectar otras paredes, si el personaje choca con esta tiene que cambiar de rotacion
    }
}
